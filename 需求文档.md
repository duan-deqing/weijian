# 公众号写作工具 — 需求文档

**产品名**：微笺  
**形态**：纯前端 SPA（Vite 构建）  
**技术**：React 18 + TypeScript + Vite + marked  
**适配**：桌面分栏 / 移动端 Tab 切换  

---

## 1. 背景与目标

微信公众号编辑器对富文本粘贴不友好：样式易被剥离、代码块与表格难看、写作过程无法专注。作者需要一个「写 Markdown → 实时预览公众号观感 → 一键复制带内联样式的 HTML」的轻量工具。

### 产品目标

| 目标 | 说明 |
|------|------|
| 写得顺 | 左侧 Markdown 编辑器，支持常用快捷插入 |
| 看得像 | 右侧预览贴近微信图文正文排版 |
| 用得上 | 复制为可粘贴进公众号编辑器的内联 HTML |
| 不丢稿 | localStorage 自动保存，刷新可恢复 |
| 随身写 | 手机浏览器可用，布局不横溢、触控友好 |

### 非目标（本期不做）

- 后端、账号体系、云同步
- 直接调用微信开放接口发文
- 图床上传（仅提示需先上传素材）
- 多人协作、评论

---

## 2. 用户与场景

| 角色 | 场景 |
|------|------|
| 公众号运营 / 自媒体作者 | 通勤或工位起草周报、推文，预览后复制到公众号后台 |
| 技术作者 | 需要代码块、列表、引用在公众号里仍可读 |
| 移动端用户 | 手机上改稿、预览、复制 |

---

## 3. 功能需求

### 3.1 编辑器（P0）

- Markdown 文本编辑（textarea，可控受控组件）
- 工具栏插入：加粗、斜体、删除线、标题、引用、无序/有序列表、链接、图片占位、代码块、分割线、表格模板
- 字数统计（字符数 / 预估阅读分钟，按 400 字/分钟）
- 快捷键：`Ctrl/Cmd + B` 加粗、`Ctrl/Cmd + I` 斜体、`Ctrl/Cmd + S` 提示已保存
- 空状态引导：首次打开展示示例 Markdown，可「清空重写」

### 3.2 预览（P0）

- 实时解析 Markdown → HTML（防抖约 150ms）
- 预览容器模拟公众号正文：最大内容宽约 578px 等价（移动端 100%），白底，字号 17px，行高 1.75
- 支持元素：h1–h3、段落、粗斜删、链接、引用、列表、表格、代码块（浅灰底）、行内代码、分割线、图片（外链）
- 三种预览主题：
  1. **经典** — 微信默认观感，强调色偏绿
  2. **墨黑** — 无彩色、更克制的标题分隔
  3. **杂志** — 大标题、更紧的字重对比
- 桌面：编辑 / 预览左右分栏，可拖拽分割线（可选增强）
- 移动端：顶部分段控件「写作 | 预览」切换，互不同时滚动嵌套

### 3.3 导出与复制（P0）

- **复制富文本**：将预览 HTML 转为**内联 style**（公众号会剥离 class），写入剪贴板 `text/html` + `text/plain` 回退
- **复制 Markdown**：纯文本
- **导入 Markdown**：选择本地 `.md` / `.markdown` / `.txt`，创建为新草稿并载入编辑器
- **导出 .md**：按当前草稿名下载 `text/markdown`
- 复制成功后 Toast 提示；失败时提示「请手动全选预览区复制」

### 3.4 持久化（P0）

- **多草稿本地库**（`localStorage`）：
  - `weijian.drafts`：草稿数组（id / name / content / theme / updatedAt，上限 30）
  - `weijian.active`：当前草稿 id
  - `weijian.theme`：主题
- 旧版单键 `weijian.draft` 自动迁移为默认草稿
- 输入防抖保存（约 400ms）；`Ctrl/Cmd+S` 立即落盘
- 顶栏可重命名当前稿；「更多」中切换/删除历史草稿
- 顶栏显示「已保存 / 保存中」轻量状态

### 3.5 微信规范对齐（P0，实现层）

| 规范点 | 处理 |
|--------|------|
| 样式被剥离 | 复制时内联化 color / font-size / line-height / margin / padding / border / background |
| 字体 | 预览与导出使用 `-apple-system, BlinkMacSystemFont, 'Helvetica Neue', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif` |
| 正文字号 | 预览与导出统一约 17px，行高 1.75 |
| 代码 | 浅灰背景 `#f6f8fa`，等宽字体，水平可滚动 |
| 图片 | 仅支持 URL；界面文案提示「公众号需使用已上传的图片链接」 |
| 链接 | 保留 `href`；提示外链在公众号内可能不可点 |

### 3.6 移动端适配（P0）

- 断点：`max-width: 768px` 单栏 + Tab
- 触控目标 ≥ 44px
- `viewport-fit=cover`，安全区 `env(safe-area-inset-*)`
- 工具栏在小屏横向滚动，避免折行过高
- 无横向页面滚动（预览代码块内部可滚）

### 3.7 体验增强（P1，本期尽量做）

- 主题切换即时生效
- 工具栏「表格」插入 2×2 示例
- 预览区滚动独立于编辑区（桌面）
- `prefers-reduced-motion` 关闭非必要动画

### 3.8 明确不做（本期）

- 所见即所得拖拽排版
- 微信封面图、摘要、作者字段管理（可作二期）
- 暗色编辑器主题（可作二期）

---

## 4. 信息架构与界面

```
┌──────────────────────────────────────────────┐
│ 顶栏：微笺 · 主题 · 字数 · 复制富文本 · 更多   │
├──────────────┬───────────────────────────────┤
│ [写作] [预览] │  （仅移动端显示分段控件）      │
├──────────────┴───────────────────────────────┤
│ 编辑器 Markdown          │  预览（微信文章纸） │
│ 工具栏 + textarea        │  主题样式作用域      │
└──────────────────────────┴────────────────────┘
```

- **顶栏**：品牌、主题 chips、统计、主操作「复制到公众号」
- **编辑侧**：工具条 + 全高编辑区，等宽字体
- **预览侧**：居中「文章纸」卡片，模拟手机阅读感

---

## 5. 技术方案

| 项 | 选型 |
|----|------|
| 框架 | React 18 + TypeScript |
| 构建 | Vite 6 |
| Markdown | `marked`（GFM） |
| 样式 | 全局 CSS + CSS 变量 |
| 状态 | Hooks（`useDraft` / `useToast`） |
| 存储 | localStorage 多草稿 |

### 文件结构（交付）

```
wechatWritingTool/
├── 需求文档.md
├── package.json
├── tsconfig.json
├── vite.config.ts
├── index.html
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── types.ts
│   ├── components/
│   ├── hooks/
│   ├── lib/          # markdown / clipboard / storage / file / editor
│   ├── constants/
│   └── styles/
└── dist/             # npm run build 产物
```

### 常用命令

```bash
npm install
npm run dev      # 开发
npm run build    # tsc + vite build
npm run preview  # 预览 dist
```

### 剪贴板策略

1. 优先 `ClipboardItem` 写入 `text/html` + `text/plain`
2. 不支持则 `document.execCommand('copy')` 临时 contenteditable
3. 失败 Toast 给出手动复制指引

### HTML 内联化

遍历预览 DOM 节点，读取计算样式中白名单属性，写回 `style` 属性后序列化。

### 导入导出

- 导入：`<input type="file">` → `FileReader` 读 UTF-8 → `createDraft`
- 导出：`Blob` + `URL.createObjectURL` 下载；文件名来自草稿名并做非法字符清洗

---

## 6. 验收标准

1. `npm run dev` / `npm run build && npm run preview` 可运行
2. 输入 Markdown，预览即时更新
3. 「复制到公众号」后粘贴到支持 HTML 的编辑器，标题/加粗/引用/代码块样式可见
4. 刷新页面草稿仍在；可导入 `.md`、导出 `.md`
5. 375px 宽度下无横向滚动，Tab 可切换编辑与预览
6. 桌面 ≥1024px 显示双栏
7. `npm run build`（含 `tsc -b`）无错误

---

## 7. 里程碑

| 阶段 | 产出 |
|------|------|
| M1 需求 | 本文档 |
| M2 实现 | `index.html` 可运行 |
| M3 验证 | 桌面 + 模拟手机宽度手测主路径 |

---

## 8. 风险与后续

| 风险 | 缓解 |
|------|------|
| 公众号后台粘贴策略变动 | 保留 Markdown 复制与手动全选回退 |
| CDN 不可用 | 文档说明可离线替换本地依赖（二期） |
| 图片防盗链 | 界面明确提示使用公众号图床 |

**二期候选**：封面/摘要字段、暗色主题、更多排版主题、导出 HTML 文件、历史版本。
